<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Frame Customizer</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <div class="container">
        <h1>Customize Your Profile Frame</h1>

        <!-- Image Upload Section -->
        <div class="upload-section">
            <label for="photoSelect">Choose a Photo:</label>
            <select id="photoSelect">
                <option value="<a href="https://ibb.co/nf4hQNt"><img src="https://i.ibb.co/sQcLsdz/1.png" alt="1" border="0"></a>">Photo 1</option>
                <option value="custom">Upload Custom Photo</option>
            </select>
        </div>

        <!-- Image Upload Button for Custom Photo -->
        <div class="upload-section" id="customPhotoSection" style="display: none;">
            <input type="file" id="imageUpload" accept="image/*">
            <button onclick="applyPhoto()">Apply Custom Photo</button>
        </div>

        <!-- Canvas for displaying image and frame -->
        <canvas id="canvas" width="600" height="600"></canvas>

        <!-- Frame Selection -->
        <div class="frame-selector">
            <label for="frameSelect">Choose Frame:</label>
            <select id="frameSelect">
                <option value="frame1.png">Frame 1</option>
                <option value="frame2.png">Frame 2</option>
                <option value="https://i.postimg.cc/fySgRC5G/1.png">Custom Frame (Postimg)</option>
            </select>
        </div>

        <!-- Save and Share Options -->
        <div class="share-options">
            <button onclick="downloadImage()">Download Image</button>
            <button onclick="generateShareLink()">Generate Share Link</button>
        </div>

        <!-- Shareable Link -->
        <div id="shareLinkSection" style="display:none;">
            <p>Your shareable link: <a id="shareLink" href="#" target="_blank"></a></p>
        </div>
    </div>

    <script src="app.js"></script>
</body>
</html>
